import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.feria.camposoberano',
  appName: 'Feria Campo Soberano',
  webDir: 'dist'
};

export default config;
